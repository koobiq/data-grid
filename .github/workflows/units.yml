name: Unit Tests

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  units:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-node
      - run: yarn run unit
